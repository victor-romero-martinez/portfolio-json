---
import { basics } from "@cv";

interface Props {
  title: string;
}

const { title } = Astro.props;
const { summary } = basics;
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={summary} />
    <meta
      property="og:title"
      content="Portfolio de Victor Romero | Front-end Developer Junior"
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://portfolio-json-ten.vercel.app/" />
    <meta property="og:image" content="/me.webp" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --bg-color: #232946;
    --bg-secondary: #363f6c;
    --paragraph: #b8c1ec;
    --primary: #fffffe;
    --secondary: #ececec;
    --terciary: #eebbc3;
  }

  /* css reset */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
    letter-spacing: -0.025rem;
  }

  body,
  figure,
  ul {
    margin: 0;
    padding: 0;
  }

  ul,
  li {
    list-style: none;
  }

  h1,
  h2 {
    color: var(--primary);
    margin: 0;
    text-wrap: balance;
  }

  h3,
  h4 {
    margin: 0;
    text-wrap: balance;
  }

  p {
    font-family: monospace;
    text-wrap: pretty;
    line-height: 1.5;
    font-size: large;
    margin: 0;
  }

  /* my styles */
  body {
    max-width: 700px;
    background-color: var(--bg-color);
    color: var(--paragraph);
    margin-inline: auto;
  }

  .print {
    display: none !important;
  }

  @media print {
    body {
      --bg-secondary: #e2e2d6;
      --paragraph: #70706a;
      --primary: #242422;
      --secondary: #4a4a46;
      --terciary: #242422;
    }

    .no-print {
      display: none !important;
    }

    .print {
      display: block !important;
      font-size: small;
    }

    astro-dev-toolbar {
      display: none !important;
    }
  }
</style>
