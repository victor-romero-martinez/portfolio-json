<span>
  <slot />
</span>

<style>
  span {
    color: var(--terciary);
    background-color: var(--bg-secondary);
    display: inline-flex;
    align-items: center;
    gap: 4px;
    border-radius: 0.3vmax;
    font-size: 0.7rem;
    font-weight: 500;
    padding: 0.2rem 0.6rem;
  }
</style>
